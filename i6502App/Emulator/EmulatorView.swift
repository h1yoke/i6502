import SwiftUI

struct SimulatorView: View {
    var body: some View {
        Color.clear
            .background(Color(red: 41 / 255.0, green: 42 / 255.0, blue: 47 / 255.0).ignoresSafeArea())
    }
}
